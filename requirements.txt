# ==============================================================
# Python dependencies (install with: pip install -r requirements.txt)
# ==============================================================
litellm>=1.40.0


# --- API key ---
# export ANTHROPIC_API_KEY=your_key   # console.anthropic.com

# --- Model overrides (all optional) ---
# Upgrade initial/followup to Sonnet for harder targets:
# export RECONLLM_INITIAL_MODEL=claude-sonnet-4-6
# export RECONLLM_FOLLOWUP_MODEL=claude-sonnet-4-6


# ==============================================================
# System tools — REQUIRED (directly invoked by the framework)
# ==============================================================
# nmap          sudo apt install nmap
# ping          pre-installed on all platforms


# ==============================================================
# System tools — RECOMMENDED (suggested by the LLM analyst)
# ==============================================================

# Web enumeration
# gobuster      sudo apt install gobuster
# ffuf          sudo apt install ffuf
# feroxbuster   sudo apt install feroxbuster
# nikto         sudo apt install nikto
# wpscan        sudo apt install wpscan
# whatweb       sudo apt install whatweb
# wfuzz         sudo apt install wfuzz
# curl          sudo apt install curl

# Exploitation
# sqlmap        sudo apt install sqlmap
# hydra         sudo apt install hydra
# metasploit    sudo apt install metasploit-framework   (msfconsole, msfvenom)
# searchsploit  sudo apt install exploitdb

# SMB / Active Directory
# enum4linux    sudo apt install enum4linux
# smbclient     sudo apt install smbclient
# crackmapexec  sudo apt install crackmapexec
# impacket      pip install impacket                    (psexec.py, secretsdump.py, etc.)

# Vulnerability scanning
# nuclei        sudo apt install nuclei
# nmap scripts  included with nmap                      (--script=vuln, --script=http-enum, etc.)


# ==============================================================
# Install all recommended tools at once (Kali / Debian):
# sudo apt update && sudo apt install -y \
#   nmap gobuster ffuf feroxbuster nikto wpscan whatweb wfuzz \
#   curl sqlmap hydra metasploit-framework exploitdb \
#   enum4linux smbclient crackmapexec nuclei && \
# pip install impacket
# ==============================================================
